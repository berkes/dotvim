Contains sourcefiles and oddly packaged vim bundles or snippets.

* E.g. Drupal-Snippets-for-Vim requires only a subdirectory to be
  included. As such, the git-clone will clone the wrong directory
structure.

